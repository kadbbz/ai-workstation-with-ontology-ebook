# AI 如何知道"我做完了"？Agent 的任务完成判断机制

上一篇描述了 Agent Loop 的整体结构，结尾留了一个问题：Agent 怎么知道任务完成了？

这个问题比它看起来要难。在传统软件里，"完成"是一个明确的程序状态，函数返回、进程退出、状态机到达终止节点，结束条件是代码写死的。Agent Loop 里不是没有完成状态，而是这个状态通常不是由固定程序路径直接给出的，而是由 Agent 在运行时维护和判断的。

推理可以出错，而且出错的方式有时候很隐蔽。

## 两种错误的完成判断

Agent 对任务完成的判断出错，大致有两种方向。

第一种是**过早结束**。Agent 认为任务完成了，但实际上漏掉了某个子目标，或者某一步产出的结果不符合要求，但 Agent 没有发现。这种情况下用户拿到的是一个"看起来完整"的结果，里面有错误或遗漏，如果用户不仔细检查，错误会被直接带进后续决策。

采购补货方案的例子：Agent 查了库存、查了销售记录、生成了 Excel，认为任务完成。但它忘了处理用户提到的"华为手机暂停销售"这个条件，Excel 里仍然包含了华为手机的补货建议。用户收到文件，如果直接提交给采购部门，这批手机就会被采购。

第二种是**无法结束**。Agent 陷入了一个推理循环，或者在反复重试一个失败的操作，无法判断是继续等待还是放弃。这种情况更容易被发现，因为对话框里的等待转圈会一直转下去，但它同样消耗 token，浪费推理资源，严重时会触发超时错误。

两种错误往往都和完成判断机制设计有关：Agent 对"任务是否完成"的判断依赖推理，同时还受任务拆解质量、工具反馈、重试策略、终止条件设计等因素影响，所以会出错。

## 工作清单机制

一个常见的工程解法是让 Agent 显式维护一个工作清单。

任务接收之后，Agent 的第一个动作不是立刻执行，而是把任务拆解成子目标列表，相当于给自己列一个待办清单。每完成一个子目标，在清单上标记完成。循环的终止条件是：清单上所有项目都被标记为完成。

这个机制把"任务是否完成"从一个模糊的推理问题，变成了一个更容易逐项检查的问题。每一项的完成状态变得更可追踪，汇总起来的整体完成状态也更容易核对。

但它没有彻底消除出错的可能，只是把风险前移了。风险从"执行完了才发现遗漏"变成了"拆解时就漏掉了某个子目标"。如果 Agent 在最初拆解任务时，就没有识别出"华为手机暂停销售"是一个需要处理的条件，这个条件就不会出现在工作清单里，后续的完成判断再准确也无济于事。

任务拆解的质量，因此成了整个链路里最上游的质量控制点。这件事和 Prompt 的设计有很大关系，也和本体有关——如果本体里没有"商品状态"这个属性，或者没有说明"暂停销售"是商品状态的一个取值，Agent 在拆解任务时就缺少相关的概念节点，很可能忽略这个条件。

但只有工作清单还不够，还需要完成判据。比如"生成 Excel"只能说明文件产出了，不能说明任务真的完成了；还要进一步检查文件里是否已经排除了华为手机、是否具体到了 SKU、补货建议是否基于库存和销售数据得出。也就是说，子目标不仅要有"做没做"，还要有"做到什么程度才算完成"。这个判据可以写成显式检查项，也可以由独立的校验逻辑执行，但不能完全依赖 Agent 自己主观判断。

## 阶段性结果的处理

任务没有完成时，Agent 不是从头开始新一轮循环，而是把当前阶段的结果加入上下文，在这个基础上继续。

这个设计有两个作用。

1. 积累推理所需的信息。第一轮查完库存，库存数据在上下文里；第二轮查销售记录时，Agent 知道库存是什么情况，可以结合两份数据做更准确的补货量计算。如果每次循环都清空上下文重新开始，Agent 就失去了"记住前面做了什么"的能力，无法完成需要多步推理的任务。
2. 让 Agent 能够识别重复失败。如果某个子目标在连续几轮循环里都没有成功，且每次失败的原因相同，Agent 应该判断这是一个无法通过重试解决的问题，而不是继续循环。把失败记录加入上下文，是让 Agent 有机会识别这个模式的前提。

上下文的积累同时带来了一个工程问题，随着循环轮次增加，上下文窗口里的内容越来越多，token 消耗线性增长，推理延迟也随之上升。对于需要十几轮循环才能完成的复杂任务，这个成本是可观的。一些 Agent 框架的解决方式是对历史上下文做摘要压缩，也就是把早期轮次的详细记录压缩成关键结论，保留信息的同时控制长度。压缩的时机和粒度，是实现细节上的工程权衡，没有放之四海而皆准的答案。

## 人在回路的真实作用

"人在回路"这个词在 AI 讨论里出现频率很高，但它的实际含义经常被简化成"让人来审查 AI 的输出"。这个理解是对的，但不完整。

人在回路在任务完成判断这个环节上有更具体的作用。Agent 在某个决策点上不确定该怎么做时，它可以暂停循环，向用户提问，把用户的回答加入上下文，然后继续。这不是 Agent 在"请示批准"，而是在补充它自己缺少的信息。

回到补货方案的例子。Agent 查完库存，发现某几个 SKU 的库存数据存在异常——当前库存量是负数，这在正常情况下不应该出现。Agent 有两个选择：自己猜测原因并继续（可能猜错），或者向用户报告这个异常并等待指示。

选择主动提问的 Agent，在这一轮循环里没有完成对应的子目标，但它避免了在错误数据基础上继续推理。用户看到提问，意识到数据有问题，去检查系统，发现是某次入库操作录入出错，修正之后告知 Agent，Agent 继续。整个过程多花了时间，但结果的可靠性大幅提升。

这个模式的代价是用户需要保持在线，无法完全"扔给 AI 自己跑"。对于高价值、高风险的任务，这个代价通常是值得的。对于低风险的重复性任务，这个代价可能超过了人工介入带来的收益。

在很多企业场景里，人在回路还承担最终验收的职责。Agent 可以判断"我应该停了"，但是否真的可以把结果作为正式产出交出去，往往还要由业务角色确认。这一步不是为了替 Agent 擦屁股，而是把任务完成的责任闭环补上。

人在回路的设计，是在任务价值和用户可用性之间找平衡，没有固定答案，需要根据具体场景决定在哪些节点上设置人工确认，哪些节点上允许 Agent 自主决定。

## 当 Agent 反复失败时

有一类情况需要单独说：Agent 在某个子目标上连续失败，且失败原因超出了当前循环自己能解决的范围，比如系统接口返回权限错误、网络超时、业务系统返回数据格式不符合预期。

这类失败不全是同一种问题：有些更偏执行环境，有些更偏接口契约或调用条件判断错误。Agent 重试同一个操作，每次都得到相同的失败，理论上应该在 N 次之后放弃并上报，而不是无限重试。

但"N 次之后放弃"是一个需要显式设计的机制，不是 Agent 自然具备的能力。如果没有重试次数限制，Agent 可能在一个永远不会成功的操作上消耗大量 token 和时间。如果限制太严，一次偶发的网络抖动会让 Agent 过早放弃本来可以完成的任务。

合理的设计通常是：区分可重试的失败（网络超时、临时锁定）和不可重试的失败（权限错误、参数校验失败），对前者设置指数退避的重试策略，对后者立即上报用户并终止相关子目标的执行。这需要在运行时契约、工具错误码定义或 Agent 框架的错误处理逻辑里，对不同类型的失败做明确的分类。

没有这个分类，Agent 会把所有失败当成同一类问题处理，要么全部重试，要么全部放弃，两种处理方式在不同的失败类型面前都会产生错误的结果。在 OpenClaw 等主流通用型 Agent 中，这部分判断引入了 AI 来执行，虽然会有更大的 token 消耗，但效果也展现出了一定的优势。

## 完成判断和系统可信度的关系

一个不容易被注意到的问题：Agent 的任务完成判断质量，直接影响用户对这个系统的长期信任。

如果 Agent 偶尔过早结束，用户发现结果有遗漏，修正之后继续用——这在可接受范围内。但如果 Agent 频繁过早结束，用户每次收到结果都要仔细检查，检查的工作量抵消了 AI 带来的效率提升，用户就会开始质疑"用这个到底有没有比自己做省时间"。

这个信任的建立和损耗，是一个累积效应。早期的几次成功会建立信任，中途的几次明显失误会大幅拉低信任，后续的持续稳定表现能慢慢恢复，但恢复的速度通常慢于损耗的速度。

这是渐进式交付策略的底层逻辑之一：先用白名单控制 Agent 能做的事情范围，在一个小范围内把准确率做到可信，再扩展范围。宁可慢一点扩展，也不要因为过早暴露过多能力而在关键时刻出现明显失误，透支还没建立稳固的信任。

---

> 本文是"企业AI落地"系列的第16篇。下一篇从任务完成判断的讨论里跳出来，看一个更高层次的选型问题：AI Workflow 和 AI 工作台，没有优劣之分，只有场景之别，判断依据是什么。
